#include<bits/stdc++.h>
using namespace std;
vector<int>res(vector<int>&ans){
    vector<pair<int,long long>>elements;
    for(const auto& entery : freq){
        elements.push_back({entry.first})
    }
}

